# WinRun Launcher Templates

This directory contains reference assets used by the `winrun create-launcher` command. The CLI copies these templates and injects program-specific metadata to produce native macOS `.app` wrappers for Windows executables.
